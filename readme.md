git config --global user.name "kavyamamilla18"
git config --global user.email "kavyamamilla18@gmail.com"

git init
git remote add origin (https link) 
code . 
 git status (optional cmd)
 git add .
 git status
 git commit -m "custom msg here"
 git push
 git checkout -b "tag"